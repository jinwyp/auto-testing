workspace:
  base: /dist
  path: .
pipeline:
  build:
    image: node:10
    commands:
      - cd backend/src
      - npm install
      - npm run build
      - pwd
  rsync:
      image: drillster/drone-rsync
      user: root
      key: @/root/.ssh/id_rsa
      hosts:
        - 35.201.251.211
      source: ./dist/backend/src
      target: ~/github
      script:
        - pwd
