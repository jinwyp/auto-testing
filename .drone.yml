pipeline:
  build:
    image: node:10
    commands:
      - cd backend/src
      - npm install
      - npm run build
  rsync:
      image: drillster/drone-rsync
      user: jinwyp
      hosts:
        - 35.201.251.211
      source: ./src
      target: ~/github
      secrets: [ rsync_key ]
